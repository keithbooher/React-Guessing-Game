import React from 'react';

const Header = props => (

    <div className="row">
        <h1 className="col s6 offset-s4">Clicky Game</h1>
        <h5 className="col s10 offset-s1">Click on an image to earn points, but dont click on any more than once!</h5>
    </div>

);

export default Header;